#include <stdio.h>

/**
 * main - the main function of the program
 * Return: Always 0 (Success)
*/

int main(void)
{
	char a;

	for (a = 'a'; a <= 'z' ; a++)
	{
		putchar(a);
	}
	putchar('\n');

	return (0);
}
